import 'package:flutter/material.dart';

/// Sips254 "Kenyan Sunset" palette — semantic names, not raw hex,
/// so a future rebrand only touches this file.
class AppColors {
  AppColors._();

  static const Color background = Color(0xFF0B0B0B);      // Deep Black
  static const Color surfaceElevated = Color(0xFF171717);  // Dark Charcoal — cards, bottom nav
  static const Color primaryAction = Color(0xFFB82025);    // Kenyan Red — CTAs
  static const Color kenyanAccent = Color(0xFF006B3C);     // Kenyan Green — local/success
  static const Color textPrimary = Color(0xFFF7F3EA);      // Warm White
  static const Color textSecondary = Color(0xFFA7A7A7);    // Soft Grey
  static const Color premiumAccent = Color(0xFFD6B56A);    // Champagne Gold
}